import {LineChart,TrendingUp} from 'lucide-react';import type {Method} from '../types/kinetics';
export default function MethodSelector({method,setMethod}:{method:Method|null;setMethod:(m:Method)=>void}){return <section><h2 className="mb-4 text-xl font-semibold">Step 1 — Select method</h2><div className="grid gap-4 md:grid-cols-2">{([['integral','Integral Method','Test integrated laws from zero through fifth order.',LineChart],['differential','Differential Method','Estimate rates and obtain order from ln(rate) vs ln[A].',TrendingUp]] as const).map(([id,title,desc,Icon])=><button key={id} onClick={()=>setMethod(id)} className={`rounded-2xl border p-5 text-left transition hover:-translate-y-0.5 hover:shadow-md ${method===id?'border-indigo-500 bg-indigo-50 ring-2 ring-indigo-200 dark:bg-indigo-950/30 dark:ring-indigo-900':'border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900'}`}><div className="mb-3 flex items-center gap-3"><Icon className="text-indigo-600"/><span className="font-semibold">{title}</span></div><p className="text-sm text-slate-600 dark:text-slate-400">{desc}</p></button>)}</div></section>}
