import {LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer,ReferenceLine} from 'recharts';import type {IntegralCandidate,KineticsResult} from '../types/kinetics';
function Plot({title,xLabel,yLabel,x,y,predicted,r2,highlight}:{title:string;xLabel:string;yLabel:string;x:number[];y:number[];predicted:number[];r2:number;highlight?:boolean}){const data=x.map((v,i)=>({x:v,y:y[i],fit:predicted[i]}));return <div className={`rounded-2xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900 ${highlight?'ring-2 ring-indigo-500':''}`}><div className="mb-2 flex items-center justify-between"><h3 className="font-semibold">{title}</h3><span className="text-xs text-slate-500">R² = {r2.toFixed(5)}</span></div><div className="h-64"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{top:10,right:15,left:0,bottom:10}}><CartesianGrid strokeDasharray="3 3" opacity={.25}/><XAxis dataKey="x" label={{value:xLabel,position:'insideBottom',offset:-5}}/><YAxis label={{value:yLabel,angle:-90,position:'insideLeft'}}/><Tooltip/><Line type="monotone" dataKey="y" name="Experimental" strokeWidth={0} dot={{r:4}}/><Line type="linear" dataKey="fit" name="Best fit" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></div></div>}
export default function GraphCard({result}:{result:KineticsResult}){if(result.method==='integral'){return <section className="space-y-4"><h2 className="text-xl font-semibold">Graphical analysis</h2><div className="grid gap-4 lg:grid-cols-3">{result.candidates!.map((c:IntegralCandidate)=><Plot key={c.order} title={`${c.label} plot`} xLabel={c.xLabel} yLabel={c.yLabel} x={c.x} y={c.y} predicted={c.predicted} r2={c.rSquared} highlight={c.order===result.reactionOrder}/>)}</div></section>}return <section className="space-y-4"><h2 className="text-xl font-semibold">Graphical analysis</h2><Plot title="Differential method: ln(rate) vs ln[A]" xLabel={result.xLabel} yLabel={result.yLabel} x={result.x} y={result.y} predicted={result.regression.predicted} r2={result.rSquared}/></section>}
