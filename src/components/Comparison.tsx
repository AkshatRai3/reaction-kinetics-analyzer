import type {KineticsResult} from '../types/kinetics';import {formatNumber} from '../utils/calculations';export default function Comparison({result}:{result:KineticsResult}){return <section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="mb-4 text-xl font-semibold">Detailed analysis</h2>{result.method==='integral'?<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b dark:border-slate-800"><th className="p-2 text-left">Order</th><th className="p-2 text-left">R²</th><th className="p-2 text-left">Slope</th><th className="p-2 text-left">Intercept</th><th className="p-2 text-left">Rate constant</th></tr></thead><tbody>{result.candidates!.map(c=><tr key={c.order} className={`border-b last:border-0 dark:border-slate-800 ${c.order===result.reactionOrder?'bg-indigo-50 dark:bg-indigo-950/20':''}`}><td className="p-2">{c.order}</td><td className="p-2">{formatNumber(c.rSquared,5)}</td><td className="p-2">{formatNumber(c.slope)}</td><td className="p-2">{formatNumber(c.intercept)}</td><td className="p-2">{formatNumber(c.k)}</td></tr>)}</tbody></table></div>:<div className="grid gap-3 sm:grid-cols-3"><div><span className="text-sm text-slate-500">Slope (order)</span><div className="font-semibold">{formatNumber(result.regression.slope,5)}</div></div><div><span className="text-sm text-slate-500">Intercept ln(k)</span><div className="font-semibold">{formatNumber(result.regression.intercept,5)}</div></div><div><span className="text-sm text-slate-500">R²</span><div className="font-semibold">{formatNumber(result.rSquared,5)}</div></div></div>}</section>}
