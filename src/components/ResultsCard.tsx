import type {KineticsResult,Units} from '../types/kinetics';import {formatNumber} from '../utils/calculations';import {rateConstantUnit} from '../utils/unitConversion';
export default function ResultsCard({result,units}:{result:KineticsResult;units:Units}){const order=result.method==='differential'?`${formatNumber(result.reactionOrder,3)} order`:`${['Zero','First','Second','Third','Fourth','Fifth'][result.reactionOrder]} order`;return <section className="space-y-4"><h2 className="text-xl font-semibold">Step 3 — Results</h2><div className="grid gap-4 sm:grid-cols-4"><Card label="Reaction Order" value={order}/><Card label="Rate Constant" value={`${formatNumber(result.rateConstant)} ${rateConstantUnit(result.reactionOrder,units.time,units.concentration)}`}/><Card label="R²" value={formatNumber(result.rSquared,5)}/><Card label="Method" value={result.method==='integral'?'Integral':'Differential'}/></div><div className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="text-sm font-medium text-slate-500">Fitted equation</div><div className="mt-2 break-words font-mono text-sm">{result.equation}</div></div>{result.excludedPoints&&result.excludedPoints.length>0&&<p className="text-sm text-amber-700 dark:text-amber-300">{result.excludedPoints.length} point(s) were excluded because the calculated rate was not positive, so its logarithm was undefined.</p>}</section>}
function Card({label,value}:{label:string;value:string}){return <div className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="text-sm text-slate-500">{label}</div><div className="mt-2 text-xl font-bold">{value}</div></div>}
